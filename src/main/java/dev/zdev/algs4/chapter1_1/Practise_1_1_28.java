package dev.zdev.algs4.chapter1_1;

public class Practise_1_1_28 {
    // public static int rank(int key, int[] a) {
    // int lo = 0;
    // int hi = a.length - 1;
    // while (lo <= hi) {
    // // Key is in a[lo..hi] or not present.
    // int mid = lo + (hi - lo) / 2;
    // if (key < a[mid])
    // hi = mid - 1;
    // else if (key > a[mid])
    // lo = mid + 1;
    // else
    // return mid;
    // }
    // return -1;
    // }

    // public static void main(String[] args) {
    // Integer[] whitelist =
    // Arrays.stream(In.readInts(args[0])).boxed().toArray(Integer[]::new);
    // TreeSet<Integer> set = new TreeSet<Integer>();
    // set.addAll(Arrays.asList(whitelist));
    // int[] array = set.toArray();
    // while (!StdIn.isEmpty()) {
    // int key = StdIn.readInt();
    // if (rank(key, whitelist) < 0)
    // StdOut.println(key);
    // }
    // }
}
